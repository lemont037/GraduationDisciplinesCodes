#include <queue> 
<template t>
*t quicksort(t vector[]){
	//Calculing the size
	int size = sizeof(vector)/sizeof(t);

	//Queue of partitions
	Index:[];
	struct part{
		int a;
		int b;
	}
	
	//Inserting the first index
	part first;
	first.a = 0;
	first.b = size - 1;
	enqueue(Index,first);

	while(Index){
		int medium_dot(Index)
	}
	
	
}

int medium_dot(int a, int b){
	return a/b;
}
